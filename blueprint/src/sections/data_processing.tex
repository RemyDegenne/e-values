\chapter{Data processing inequality with E-variables}


\section{Maximal logarithmic utility}
\label{sec:maximal_logarithmic_utility}


\begin{definition}\label{def:maxUtility}
The maximal utility of a probability measure $P \in \mathcal{P}(\mathcal{X})$ with respect to a set of probability measures $\mathcal{Q} \subseteq \mathcal{P}(\mathcal{X})$ for a function $U : \mathbb{R}_{+,\infty} \to \overline{\mathbb{R}}$ is defined as
\begin{align*}
  \mathcal{U}(P, \mathcal{Q})
  &= \sup_{f \in \mathcal{E}_{\mathcal{Q}}} P[U \circ f]
  \: .
\end{align*}
Often, $U = \log$ will be used, in which case we talk about the maximal logarithmic utility.
\end{definition}


\begin{theorem}[Minimax theorem, Theorem 2 of \cite{fan1953minimax}]\label{thm:minimax}
Let $X$ be a compact Hausdorff space and $Y$ an arbitrary set (not topologized).
Let $f$ be a real-valued function on $X \times Y$ such that, for every $y \in Y$, $f(x, y)$ is lower semi continuous on $X$.
If $f$ is convex on $X$ and concave on $Y$, then
\begin{align*}
  \inf_{x \in X} \sup_{y \in Y} f(x, y)
  &= \sup_{y \in Y} \inf_{x \in X} f(x, y)
  \: .
\end{align*}
\end{theorem}

\begin{proof}

\end{proof}

TODO: we should extend that to functions with values in $\mathbb{R} \cup \{-\infty\}$.

% \begin{theorem}[Sion's minimax theorem, version of \cite{komiya1988elementary}]\label{thm:sion}
% Let $X$ be a compact convex subset of a topological vector space and $Y$ a convex subset of a topological vector space.
% Let $f$ be a real-valued function on $X \times Y$ such that
% \begin{enumerate}
%   \item for all $x \in X$, $f(x, \cdot)$ is upper semi continuous and quasi-concave on $Y$,
%   \item for all $y \in Y$, $f(\cdot, y)$ is lower semi continuous and quasi-convex on $X$.
% \end{enumerate}
% Then
% \begin{align*}
%   \inf_{x \in X} \sup_{y \in Y} f(x, y)
%   &= \sup_{y \in Y} \inf_{x \in X} f(x, y)
%   \: .
% \end{align*}
% \end{theorem}
% Note that we could also assume that $Y$ is compact instead of $X$.

% \begin{proof}

% \end{proof}

% TODO: extend that theorem from $f$ real-valued to $f$ taking values in $\overline{\mathbb{R}}$. $\to$ I checked, it works.

% That means that in the following lemma, we can consider $P[U \circ f]$ as a value in $\overline{\mathbb{R}}$.
% That is, $P[g] = \int g⁺ dP - \int g⁻ dP$ (with not both infinite at the point where we apply the Fatou lemma).


\begin{lemma}\label{lem:minimax_limsup_le}
  \uses{def:maxUtility}
Let $U : \mathbb{R}_{+,\infty} \to \overline{\mathbb{R}}$ be a continuous, increasing, concave function finite on $(0, \infty)$ and such that for $K>0$, $\int_{K}^\infty \frac{1}{U^{-1}(x)} dx < \infty$ and tends to 0 as $K \to \infty$ (for example $U = \log$).
Let $\mathcal{G}$ be a set of measurable functions from $\mathcal{X}$ to $\mathbb{R}_{+, \infty}$ and
let $P$ be a probability distribution on $\mathcal{X}$ such that $\sup_{f \in \mathcal{G}}P[f]$ is finite.
Then for all sequences $(f_n)_{n \in \mathbb{N}}$ of functions in $\mathcal{G}$, we have
\begin{align*}
  \limsup_{n \to \infty} P[U \circ f_n]
  &\le P[\limsup_{n \to \infty} U \circ f_n]
  \: .
\end{align*}
\end{lemma}

\begin{proof}
We check that the sequence of negative parts of the functions $-U \circ f_n$ is uniformly integrable in order to apply Fatou's lemma \cite{feinberg2020fatou}.
For $f \in \mathcal{G}$, that negative part is $(-U \circ f)^{-} = \max\{0, U \circ f\}$. For $K \ge 0$,
\begin{align*}
  P[\max\{0, U \circ f\} \mathbb{I}_{\max\{0, U \circ f\} \ge K}]
  &= P[ U \circ f \mathbb{I}_{U \circ f \ge K}]
  \\
  &= \int_{K}^\infty P\{U(f(x)) \ge x\} dx
  \\
  &= \int_{K}^\infty P\{f(x) \ge U^{-1}(x)\} dx
  \\
  &\le \int_{K}^\infty \frac{1}{U^{-1}(x)} P[f] dx
  \\
  &= \left(\int_{K}^\infty \frac{1}{U^{-1}(x)} dx\right) P[f]
  \: ,
\end{align*}
in which the inequality is due to Markov's inequality.
Then since $(P[f_n])_{n \in \mathbb{N}}$ is bounded from above (and below since $f \ge 0$) and $\int_{K}^\infty \frac{1}{U^{-1}(x)} dx \to_{K \to \infty} 0$, we have uniform integrability.
\end{proof}


Remark on the topology of pointwise convergence: for $X$ a set and $Y$ a Hausdorff space, the set of functions from $X$ to $Y$ is Hausdorff for the topology of pointwise convergence.
A set $A$ of functions is compact in that topology if and only if it is closed and each set $A_x = \{f(x) \mid f \in A\}$ is compact.


\begin{theorem}\label{thm:sion_maxUtility}
Let $U : \mathbb{R}_{+,\infty} \to \overline{\mathbb{R}}$ be a continuous, increasing, concave function finite on $(0, \infty)$ and such that for $K>0$, $\int_{K}^\infty \frac{1}{U^{-1}(x)} dx < \infty$ and tends to 0 as $K \to \infty$ (for example $U = \log$).
Let $\mathcal{G}$ be a convex set of measurable functions from $\mathcal{X}$ to $\mathbb{R}_{+, \infty}$, which is compact in the topology of pointwise convergence.
Let $\mathcal{P}$ be a convex set of probability distributions on $\mathcal{X}$ such that for all $P \in \mathcal{P}$, $\sup_{f \in \mathcal{G}}P[f]$ is finite.
Then
\begin{align*}
  \inf_{P \in \mathcal{P}} \sup_{f \in \mathcal{G}} P[U \circ f]
  &= \sup_{f \in \mathcal{G}} \inf_{P \in \mathcal{P}} P[U \circ f]
  \: .
\end{align*}
\end{theorem}

\begin{proof}
  \uses{thm:minimax, lem:minimax_limsup_le}
This is an application of the minimax theorem~\ref{thm:minimax} to the function $-P[U \circ f]$, for which we need to check the hypotheses.
$P, f \mapsto P[U \circ f]$ is linear in $P$ (hence convex) and concave in $f$ by concavity of $U$.
It only remains to check that $f \mapsto P[U \circ f]$ is upper semi-continuous.
By Lemma~\ref{lem:minimax_limsup_le}, for a sequence $(f_n)$ of functions in $\mathcal{G}$ which converges pointwise to $f \in \mathcal{G}$ we have
\begin{align*}
  \limsup_{n \to \infty} P[U \circ f_n]
  &\le P[\limsup_{n \to \infty} U \circ f_n]
  = P[U \circ f]
  \: .
\end{align*}
\end{proof}


\begin{lemma}\label{lem:minimax_fin_gen}
Let $\Phi = \{\phi_1, \ldots, \phi_d\}$ be a set of measurable functions from $\mathcal{X}$ to $\mathbb{R}$.
Let $\Lambda = \{\lambda \in \mathbb{R}_+^d \mid \forall x, 1 + \sum_i \lambda_i \phi_i(x) \ge 0\}$ and assume that it is a compact set.
Let $\mathcal{R}$ be a convex set of probability measures on $\mathcal{X}$ such that for all $R \in \mathcal{R}$ and $i \in [d]$, $R[\vert \phi_i \vert] < \infty$.
Then
\begin{align*}
  \inf_{R \in \mathcal{R}} \sup_{\lambda \in \Lambda} R[\log (1 + \sum_i \lambda_i \phi_i)]
  &= \sup_{\lambda \in \Lambda} \inf_{R \in \mathcal{R}} R[\log (1 + \sum_i \lambda_i \phi_i)]
  \: .
\end{align*}
\end{lemma}

\begin{proof}
  \uses{thm:sion_maxUtility}
We use Theorem~\ref{thm:sion_maxUtility} with $U(x) = \log(x)$ and $\mathcal{G} = \{f \mid \exists \lambda \in \Lambda, f = 1 + \sum_i \lambda_i \phi_i\}$.
That set is convex and compact in the topology of pointwise convergence.
We need to check the condition on the integrals against measures in $\mathcal{R}$: for $R \in \mathcal{R}$ and $f = 1 + \sum_i \lambda_i \phi_i$,
\begin{align*}
  R[f]
  &= 1 + \sum_i \lambda_i R[\phi_i]
  \le 1 + \sup_{\lambda \in \Lambda} \Vert \lambda \Vert_\infty \sum_i R[\vert \phi_i \vert]
  < \infty
  \: .
\end{align*}

\end{proof}


\section{Data-processing inequality for e-variables}
\label{sec:data_processing_eVar}


\begin{lemma}\label{lem:randEVar_comp}
  \uses{def:randEVar}
  \leanok
  \lean{ProbabilityTheory.IsEVar.comp, ProbabilityTheory.IsRandEVar.comp}
Let $\mathcal{Q} \subseteq \mathcal{P}(\mathcal{X})$ be a set of probability measures and let $\phi : \mathcal{X} \to \mathcal{Y}$ be a measurable function and $\kappa : \mathcal{X} \rightsquigarrow \mathcal{Y}$ be a Markov kernel.
Then
\begin{enumerate}
  \item for all $g \in \mathcal{E}_{\phi \circ \mathcal{Q}}$, $g \circ \phi \in \mathcal{E}_{\mathcal{Q}}$,
  \item for all $\xi \in \mathcal{E}_{\kappa \circ \mathcal{Q}}^R$, $\xi \circ \kappa \in \mathcal{E}^R_{\mathcal{Q}}$.
\end{enumerate}
\end{lemma}

\begin{proof}\leanok
For $Q \in \mathcal{Q}$,
\begin{align*}
  Q[g \circ \phi]
  &= (\phi \circ Q)[g]
  \le 1
  \: , \\
  ((\xi \circ \kappa) \circ Q)[\id]
  &= (\xi \circ (\kappa \circ Q))[\id]
  \le 1
  \: .
\end{align*}
\end{proof}


\begin{theorem}[Data processing inequality with e-variables]\label{thm:dataprocessing}
  \uses{def:eVar}
  \leanok
  \lean{ProbabilityTheory.maxUtility_map_le, ProbabilityTheory.maxRandUtility_comp_le,
    ProbabilityTheory.maxUtility_comp_le}
Let $\mathcal{Q} \subseteq \mathcal{P}(\mathcal{X})$ be a set of probability measures and $P \in \mathcal{P}(\mathcal{X})$.
Let $U : \mathbb{R}_{+,\infty} \to \overline{\mathbb{R}}$.
Then for all measurable functions $\phi : \mathcal{X} \to \mathcal{Y}$ and all Markov kernels $\kappa : \mathcal{X} \rightsquigarrow \mathcal{Y}$,
\begin{align*}
  \sup_{f \in \mathcal{E}_{\mathcal{Q}}} P[U \circ f]
  &\ge \sup_{g \in \mathcal{E}_{\phi \circ \mathcal{Q}}} (\phi \circ P)[U \circ g]
  \: , \\
  \sup_{\eta \in \mathcal{E}^R_{\mathcal{Q}}} (\eta \circ P)[U]
  &\ge \sup_{\xi \in \mathcal{E}^R_{\kappa \circ \mathcal{Q}}} (\xi \circ \kappa \circ P)[U]
  \: .
\end{align*}
Furthermore, if $U$ is concave, then for all Markov kernels $\kappa : \mathcal{X} \rightsquigarrow \mathcal{Y}$,
\begin{align*}
  \sup_{f \in \mathcal{E}_{\mathcal{Q}}} P[U \circ f]
  &\ge \sup_{g \in \mathcal{E}_{\kappa \circ \mathcal{Q}}} (\kappa \circ P)[U \circ g]
  \: .
\end{align*}
For $\phi$ a measurable embedding, the inequalities are equalities.
\end{theorem}

\begin{proof}
  \uses{lem:randEVar_comp, lem:sup_randEVar}
By Lemma~\ref{lem:randEVar_comp}, $\{\xi \circ \kappa \mid \xi \in \mathcal{E}^R_{\kappa \circ \mathcal{Q}}\} \subseteq \mathcal{E}^R_{\mathcal{Q}}$, hence
\begin{align*}
  \sup_{\eta \in \mathcal{E}^R_{\mathcal{Q}}} (\eta \circ P)[U]
  &\ge \sup_{\eta \in \{\xi \circ \kappa \mid \xi \in \mathcal{E}^R_{\kappa \circ \mathcal{Q}}\}} (\eta \circ P)[U]
  = \sup_{\xi \in \mathcal{E}^R_{\kappa \circ \mathcal{Q}}} (\xi \circ \kappa \circ P)[U]
  \: .
\end{align*}
The argument for the first inequality is similar.

If $U$ is concave, we can use Lemma~\ref{lem:sup_randEVar} to obtain the data-processing with a Markov kernel for e-variables from the result for randomized e-variables.
\end{proof}

\paragraph{KL data processing from e-variables}

The data processing inequality for the numeraire utility implies the data processing inequality for KL divergences.

\begin{theorem}\label{thm:dataprocessing_kl}
  \uses{def:eVar}
  Let $P, Q \in \mathcal{P}(\mathcal{X})$.
  Then for all Markov kernels $\kappa : \mathcal{X} \rightsquigarrow \mathcal{Y}$,
  \begin{align*}
    \KL(P, Q)
    &\ge \KL(\kappa \circ P, \kappa \circ Q)
    \: .
  \end{align*}
\end{theorem}

\begin{proof}
  \uses{lem:sup_eVar_singleton, thm:dataprocessing}
By Lemma~\ref{lem:sup_eVar_singleton}, $\KL(P, Q) = \sup_{f \in \mathcal{E}_{\{Q\}}} P[\log f]$.
Then by Theorem~\ref{thm:dataprocessing}, we have
\begin{align*}
  \sup_{f \in \mathcal{E}_{\{Q\}}} P[\log f]
  &\ge \sup_{g \in \mathcal{E}_{\{\kappa \circ Q\}}} (\kappa \circ P)[\log g]
  \: .
\end{align*}
And that last term is $\KL(\kappa \circ P, \kappa \circ Q)$, again by Lemma~\ref{lem:sup_eVar_singleton}.
\end{proof}

\section{Separation lower bound}
\label{sec:separation_lower_bound}


\begin{lemma}\label{lem:sup_quadratic}
For all $u \in (0,1)$,
\begin{align*}
  \sup_{\lambda \ge 0} \frac{1}{2}\log (1 + \lambda (u - 1)) + \frac{1}{2}\log (1 + \lambda (u + 1))
  = \frac{1}{2} \log \frac{1}{1 - u^2}
  \: .
\end{align*}
\end{lemma}

\begin{proof}
It suffices to compute the supremum of $(1 + \lambda (u - 1))(1 + \lambda (u + 1))$ over $\lambda \ge 0$, which is simply finding the maximum of a quadratic function.
\end{proof}


\begin{lemma}\label{lem:inf_symm_sup_utility}
For all $u \in (0,1)$,
\begin{align*}
  \inf_{R \in \mathcal{P}([-1,1]), \ -\id \circ R = R} \sup_{\lambda \ge 0} R[\log (1 + \lambda (X + u)]]
  = \frac{1}{2} \log \frac{1}{1 - u^2}
  \: .
\end{align*}
\end{lemma}

\begin{proof}\uses{lem:sup_quadratic}
For the first inequality, we use that the infimum is less than the value at $R = \frac{1}{2}\delta_{-1} + \frac{1}{2}\delta_{1}$.
Then by Lemma~\ref{lem:sup_quadratic}, the supremum over $\lambda \ge 0$ for that distribution is $\frac{1}{2} \log \frac{1}{1 - u^2}$.

For the second inequality, we use first the inequality $\inf\sup \ge \sup\inf$.
\begin{align*}
  &\inf_{R \in \mathcal{P}([-1,1]), \ -\id \circ R = R} \sup_{\lambda \ge 0} R[\log (1 + \lambda (X + u))]
  \\
  &\ge \sup_{\lambda \ge 0} \inf_{R \in \mathcal{P}([-1,1]), \ -\id \circ R = R} R[\log (1 + \lambda (X + u))]
  \\
  &= \sup_{\lambda \ge 0} \inf_{R \in \mathcal{P}([0,1])} \frac{1}{2} R[\log (1 + \lambda (X + u))] + \frac{1}{2}R[\log(1 + \lambda (-X + u))]
  \: .
\end{align*}
For any $\lambda \ge 0$, the infimum is achieved by a Dirac distribution $\delta_{x}$ for some $x \in [0,1]$.
We get a lower bound
\begin{align*}
  \sup_{\lambda \ge 0} \inf_{x \in [0,1]} \frac{1}{2} \log (1 + \lambda (u + x)) + \frac{1}{2} \log (1 + \lambda (u - x))
  \: .
\end{align*}
The minimizer for $x$ is at $x=1$ for all values of $\lambda$.
We get to the value
\begin{align*}
  \sup_{\lambda \ge 0} \frac{1}{2} \log (1 + \lambda (u + 1)) + \frac{1}{2} \log(1 + \lambda (u - 1))
\end{align*}
which is equal to $\frac{1}{2} \log \frac{1}{1 - u^2}$ by Lemma~\ref{lem:sup_quadratic}.
\end{proof}


\begin{lemma}\label{lem:inf_bounded_renyi_sparation}
  \uses{def:eVar}
Let $\mathcal{D}_{\le -\varepsilon} = \{P \in \mathcal{P}([-1,1]) \mid P[\id] \le -\varepsilon\}$ and $\mathcal{D}_{\ge \varepsilon} = \{P \in \mathcal{P}([-1,1]) \mid P[\id] \ge \varepsilon\}$.
Then
\begin{align*}
  \inf_{R \in \mathcal{P}([-1,1])}\left\{\sup_{f \in \mathcal{E}_{\mathcal{D}_{\le -\varepsilon}}} R[\log f]
    + \sup_{g \in \mathcal{E}_{\mathcal{D}_{\ge \varepsilon}}} R[\log g] \right\}
  &= \log \frac{1}{1 - u^2}
  \: .
\end{align*}
\end{lemma}

\begin{proof}
  \uses{lem:inf_symm_sup_utility}
The infimum can be restricted to the set of probability measures with $-id \circ R = R$ (symmetric around 0).
TODO: explain why this is the case.
\begin{align*}
  \inf_{R \in \mathcal{P}([-1,1])}\left\{\sup_{f \in \mathcal{E}_{\mathcal{D}_{\le -\varepsilon}}} R[\log f]
    + \sup_{g \in \mathcal{E}_{\mathcal{D}_{\ge \varepsilon}}} R[\log g] \right\}
  &= 2\inf_{R \in \mathcal{P}([-1,1]), \ -\id \circ R = R} \sup_{f \in \mathcal{E}_{\{P \mid P[\id] \le -\varepsilon\}}} R[\log f]
  \\
  &= 2\inf_{R \in \mathcal{P}([-1,1]), \ -\id \circ R = R} \sup_{\lambda \ge 0} R[\log (1 + \lambda (X + u)]]
  \: .
\end{align*}
TODO: add result that explains the last line.

Then use Lemma~\ref{lem:inf_symm_sup_utility}.
\end{proof}


\begin{lemma}\label{lem:utility_fin_gen_ge}
  \uses{def:eVar}
Let $\Phi = \{\phi_1, \ldots, \phi_d\}$ be a set of measurable functions from $\mathcal{X}$ to $\mathbb{R}$.
Let $\mathcal{P}^\Phi = \{P \in \mathcal{P}(\mathcal{X}) \mid \forall \phi \in \Phi, \ P[\vert \phi \vert] < \infty \text{ and } P[\phi] \le 0\}$.
Denote by $\phi : \mathcal{X} \to \mathbb{R}^d$ the vector-valued function $\phi(x) = (\phi_1(x), \ldots, \phi_d(x))$.
Then
\begin{align*}
  \sup_{f \in \mathcal{E}_{\mathcal{P}^\Phi}}R[\log f]
  \ge \sup_{f \in \mathcal{E}_{\{P \in \mathcal{P}(\mathbb{R}^d) \mid \forall i, P[X_i] \le 0\}}}(\phi \circ R)[\log f]
  \; .
\end{align*}
\end{lemma}

\begin{proof}
  \uses{thm:dataprocessing, lem:eVarSet_anti}
By Theorem~\ref{thm:dataprocessing} for the function $\phi$, we have
\begin{align*}
  \sup_{f \in \mathcal{E}_{\mathcal{P}^{\Phi}}} R[\log f]
  &\ge \sup_{f \in \mathcal{E}_{\phi \circ \mathcal{P}^\Phi}} (\phi \circ R)[\log f]
  \: .
\end{align*}
$\phi \circ \mathcal{P}^\Phi \subseteq \{P \in \mathcal{P}(\mathbb{R}^d) \mid \forall i, P[X_i] \le 0\}$ and their e-variable sets are in the reversed order (Lemma~\ref{lem:eVarSet_anti}).
We get the result of the lemma.
\end{proof}


\begin{lemma}\label{lem:todo2}
Let $\mathcal{P},\mathcal{Q} \subseteq \mathcal{P}(\mathcal{X})$ be two sets of probability measures.
Suppose that there exists a measurable function $f_0 : \mathcal{X} \to [-1, 1]$ and $\varepsilon \in (0,1]$ such that
\begin{align*}
  \forall P \in \mathcal{P}, \: P[f_0] &\le - \varepsilon
  \: , \\
  \forall Q \in \mathcal{Q}, \: Q[f_0] &\ge \varepsilon
  \: .
\end{align*}
Then
\begin{align*}
  \inf_{R \in \mathcal{P}(\mathcal{X})}\left\{\sup_{f \in \mathcal{E}_{\mathcal{P}}} R[\log f]
    + \sup_{g \in \mathcal{E}_{\mathcal{Q}}} R[\log g] \right\}
  \ge \inf_{R \in \mathcal{P}([-1,1])}\left\{\sup_{f \in \mathcal{E}_{\{P \mid P[\id] \le -\varepsilon\}}} R[\log f]
  + \sup_{g \in \mathcal{E}_{\{Q \mid Q[\id] \ge \varepsilon\}}} R[\log g] \right\}
  \: .
\end{align*}
\end{lemma}

\begin{proof}
  \uses{thm:dataprocessing, lem:eVarSet_anti}
By Theorem~\ref{thm:dataprocessing} for the function $f_0$, we have
\begin{align*}
  \inf_{R \in \mathcal{P}(\mathcal{X})}\left\{\sup_{f \in \mathcal{E}_{\mathcal{P}}} R[\log f]
    + \sup_{g \in \mathcal{E}_{\mathcal{Q}}} R[\log g] \right\}
  &\ge \inf_{R \in \mathcal{P}(\mathcal{X})}\left\{\sup_{f \in \mathcal{E}_{f_0 \circ \mathcal{P}}} (f_0 \circ R)[\log f]
    + \sup_{g \in \mathcal{E}_{f_0 \circ \mathcal{Q}}} (f_0 \circ R)[\log g] \right\}
  \\
  &\ge \inf_{R \in \mathcal{P}([-1,1])}\left\{\sup_{f \in \mathcal{E}_{f_0 \circ \mathcal{P}}} R[\log f]
    + \sup_{g \in \mathcal{E}_{f_0 \circ \mathcal{Q}}} R[\log g] \right\}
  \: .
\end{align*}
$f_0 \circ \mathcal{P} \subseteq \{P \in \mathcal{P}([0,1]) \mid P[\id] \le -\varepsilon\}$ and their e-variable sets are in the reversed order (Lemma~\ref{lem:eVarSet_anti}).
A similar inclusion holds for $f_0 \circ \mathcal{Q}$.
We get the result of the lemma.
\end{proof}


\begin{lemma}\label{lem:todo}
Let $\mathcal{P},\mathcal{Q} \subseteq \mathcal{P}(\mathcal{X})$ be two sets of probability measures.
Suppose that there exists a measurable function $f_0 : \mathcal{X} \to [-1, 1]$ and $\varepsilon \in (0,1]$ such that
\begin{align*}
  \forall P \in \mathcal{P}, \: P[f_0] &\le - \varepsilon
  \: , \\
  \forall Q \in \mathcal{Q}, \: Q[f_0] &\ge \varepsilon
  \: .
\end{align*}
Then
\begin{align*}
  \inf_{R \in \mathcal{P}(\mathcal{X})}\left\{\sup_{f \in \mathcal{E}_{\mathcal{P}}} R[\log f]
    + \sup_{g \in \mathcal{E}_{\mathcal{Q}}} R[\log g] \right\}
  &\ge \log \frac{1}{1 - \varepsilon^2}
  \: .
\end{align*}
\end{lemma}

\begin{proof}
  \uses{lem:inf_bounded_renyi_sparation, lem:todo2}
Apply Lemma~\ref{lem:todo2}, followed by Lemma~\ref{lem:inf_bounded_renyi_sparation}.
\end{proof}

\begin{theorem}\label{thm:separation_lower_bound}
  \uses{def:eVar}
Let $\mathcal{P},\mathcal{Q} \subseteq \mathcal{P}(\mathcal{X})$ be two sets of probability measures.
Suppose that there exists a measurable function $f_0 : \mathcal{X} \to [0, 1]$ and $\delta \in (0,1/2)$ such that
\begin{align*}
  \forall P \in \mathcal{P}, \: P[f_0] &\le \delta
  \: , \\
  \forall Q \in \mathcal{Q}, \: Q[f_0] &\ge 1 - \delta
  \: .
\end{align*}
Then
\begin{align*}
  \inf_{R \in \mathcal{P}(\mathcal{X})}\left\{\sup_{f \in \mathcal{E}_{\mathcal{P}}} R[\log f]
    + \sup_{g \in \mathcal{E}_{\mathcal{Q}}} R[\log g] \right\}
  &\ge \log \frac{1}{4\delta(1 - \delta)}
  \: .
\end{align*}
\end{theorem}

This is analogous to a lower bound on the Rényi divergence with parameter $\alpha = 1/2$.

\begin{proof}
  \uses{lem:todo}
The function $\tilde{f}_0 : \mathcal{X} \to [-1, 1]$ defined by $\tilde{f}_0(x) = 2f_0(x) - 1$ satisfies the assumptions of Lemma~\ref{lem:todo} with $\varepsilon = 1 - 2 \delta$.
We get
\begin{align*}
  \inf_{R \in \mathcal{P}(\mathcal{X})}\left\{\sup_{f \in \mathcal{E}_{\mathcal{P}}} R[\log f]
    + \sup_{g \in \mathcal{E}_{\mathcal{Q}}} R[\log g] \right\}
  &\ge \log \frac{1}{1 - (1 - 2\delta)^2}
  = \log \frac{1}{4\delta(1 - \delta)}
  \: .
\end{align*}
\end{proof}


\begin{lemma}\label{lem:}
DRAFT. $g\circ g = \id$. $g \circ \mathcal{P} = \mathcal{Q}$.
\begin{align*}
  \inf_{R : g \circ R = R} \sup_{f \in \mathcal{E}_{\mathcal{Q}}} R[\log f]
  &\ge \sup_{f \in \mathcal{E}_{\mathcal{Q}}} \inf_{R : g \circ R = R} R[\log f]
  \\
  &= \sup_{f \in \mathcal{E}_{\mathcal{Q}}} \inf_{R}
    \left(\frac{1}{2}R[\log f] + \frac{1}{2} R[\log(f \circ g)]\right)
  \\
  &= \sup_{f \in \mathcal{E}_{\mathcal{Q}}} \inf_{x}
    \left(\frac{1}{2}\log(f(x)) + \frac{1}{2} \log(f(g(x)))\right)
\end{align*}
For any $x$,
\begin{align*}
  \inf_{R : g \circ R = R} \sup_{f \in \mathcal{E}_{\mathcal{Q}}} R[\log f]
  &\le \sup_{f \in \mathcal{E}_{\mathcal{Q}}} \frac{1}{2}\log f(x) + \frac{1}{2} \log f(g(x))
\end{align*}
\begin{align*}
  \inf_{R : g \circ R = R} \sup_{f \in \mathcal{P}^{\Phi}} R[\log f]
  &= \inf_{R : g \circ R = R} \sup_{\lambda_1, \ldots, \lambda_d \ge 0} R[\log(1 + \sum_i \lambda_i \phi_i)]
  \\
  &\ge \sup_{\lambda_1, \ldots, \lambda_d \ge 0} \inf_{R : g \circ R = R} R[\log(1 + \sum_i \lambda_i \phi_i)]
  \\
  &= \sup_{\lambda_1, \ldots, \lambda_d \ge 0} \inf_{R}
    \left(\frac{1}{2}R[\log(1 + \sum_i \lambda_i \phi_i)] + \frac{1}{2} R[\log(1 + \sum_i \lambda_i \phi_i \circ g)]\right)
  \\
  &= \sup_{\lambda_1, \ldots, \lambda_d \ge 0} \inf_{x}
    \left(\frac{1}{2}\log(1 + \sum_i \lambda_i \phi_i(x)) + \frac{1}{2} \log(1 + \sum_i \lambda_i \phi_i(g(x)))\right)
\end{align*}
\end{lemma}

\begin{proof}

\end{proof}


\section{Data processing inequality through KL duality}

\begin{align*}
  \sup_{f \in \mathcal{E}_{\mathcal{Q}}} P[\log f]
  &= \inf_{Q \in \mathcal{Q}_{\eff}} \KL(P, Q)
  \\
  &\ge \inf_{Q \in \mathcal{Q}_{\eff}} \KL(\kappa \circ P, \kappa \circ Q)
  \\
  &= \inf_{Q' \in \kappa \circ \mathcal{Q}_{\eff}} \KL(\kappa \circ P, Q')
  \\
  &\ge \inf_{Q' \in (\kappa \circ \mathcal{Q})_{\eff}} \KL(\kappa \circ P, Q')
  \\
  &= \sup_{g \in \mathcal{E}_{\kappa \circ \mathcal{Q}}} (\kappa \circ P)[\log g]
  \: .
\end{align*}

The first inequality is the data processing inequality for KL divergence.
The second inequality comes from $\kappa \circ \mathcal{Q}_{\eff} \subseteq (\kappa \circ \mathcal{Q})_{\eff}$, which we proved in Lemma~\ref{lem:effectiveSet_comp_subset}.
